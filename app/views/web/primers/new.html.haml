%div{:class => 'ui modal primer new', :data => { :url => new_primer_path } }
  = icon :close

  %div.header= title

  %div.content
    = render '/web/primers/shared/form'

  %div.actions
    %div{:class => 'ui cansel deny button'}= t 'actions.common.cansel'
    %div{:class => 'ui approve positive button'}= t 'actions.common.save'
